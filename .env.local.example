# Vercel KV 接続情報
# 初期段階: Aisle KV を共有（Vercel Dashboard で KV を refbase プロジェクトにリンク後、vercel env pull で取得）
KV_REST_API_URL=
KV_REST_API_TOKEN=
KV_REST_API_READ_ONLY_TOKEN=

# KV 移行フラグ（false = Aisle KV 共有モード / true = RefBase 専用 KV モード）
# 将来 RefBase 専用 KV に移行するときに true に切り替える
REFBASE_STANDALONE=false
